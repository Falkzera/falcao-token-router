[package]
name = "fake-claude"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false
description = "Um `claude` de mentira, só para os testes de integração da CLI `router`. Nunca empacotado."

[[bin]]
name = "fake-claude"
path = "src/main.rs"

[dependencies]
serde_json.workspace = true
