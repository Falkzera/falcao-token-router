//! Uso: `usage_percent` (a conversão fração→inteiro, num lugar só) e a sonda
//! ativa (`claude_usage_probe`). Espelha `Sources/CCUsageCore/Usage`.

pub mod claude_binary;
pub mod claude_usage_probe;
pub mod usage_percent;
